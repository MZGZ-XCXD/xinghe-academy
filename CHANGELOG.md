# 更新日志

版本号格式：`主.次.补丁`（见 `package.json` 的 `version`）。
推送到 `main` 之后 GitHub Actions 会自动跑测试、打包并发布到 GitHub Pages，所以线上永远等于 `main` 的最新状态。

改完代码记得在下面的「未发布」里补一行；发出去之后再把它归到对应的版本号下。

## 未发布

- **内容模板更友好**：`public/校园内容.示例.js` 里每个类别都加了「想多加几条 → 整段复制、改 id」的提示，课程与校规各补了第二条完整示例；`如何添加内容.md` 新增「1.5 想加第二条、第三条？」一节（含复制三步与自检方法）。新增一条测试：一次加两门课 + 两条校规，验证全部生效。
- **模板顶部新增【速查表】**：学生属性 10 个、资源 12 个（含哪些有仓储上限）、教师学科 8 个、43 个加成 `target`、以及课程/校规/科技/社团/活动/校队/卡片/成就/传承/季节的全部可取值，另附「百分比写小数」「资金与时间量级」的填值建议；用脚本与 `src/game/types.ts`、`src/data/resources.ts` 逐项核对过，全部覆盖。`如何添加内容.md` 第 8 节也指向了这份速查表。
- **新增 `npm run check:content [文件路径]`**：打开游戏之前就能体检 `校园内容.js` —— 报告语法是否通过（含出错行号）、代码位置有没有误用中文全角标点、以及一共生效了几条内容。整份文件是一段脚本，**一个标点写错就会导致整份失效**（游戏里表现为「自定义内容：未加载」），这个命令是用来兜住这类问题的。

## 0.1.1 — 2026-09-22

### 新增

- **手机端自动适配**：窗口 ≤ 820px（或触屏 ≤ 1024px）自动切「移动版」——忽略桌面缩放、三栏收成单栏、按钮加大到手指点得中、底部导航贴底不透明、悬浮提示改为**点按**弹出（触屏没有鼠标悬停），并处理了刘海屏安全区与 iOS 输入框聚焦缩放。
- **新解锁弹窗**：建筑落成 / 科技完成 / 评级考核通过 / 社团挂牌 / 校队组建 / 事件结算之后，用一段校园味的剧情句在屏幕中央说明「做了什么 → 于是解锁了什么」，逐条列出建筑 / 课程 / 科技 / 校规 / 活动 / 社团（含课程槽位 +1）。文案模板在 `src/game/engine/UnlockEngine.ts`。
- **`.gitignore` 保护个性化内容**：`public/校园内容.js` 与 `outputs/校园内容.js` 不再进仓库（仓库里只保留公开模板 `public/校园内容.示例.js`），新增 `npm run content:init` 生成自己的工作副本。
- **`CHANGELOG.md`**（本文件）与 README 里的「放到 GitHub / GitHub Pages」说明。

### 变更

- 界面默认 **175% 缩放**（可在【选项】→「界面大小」调 100%–250%），并让响应式断点按「缩放后的等效宽度」判断，放大后仍是三栏。
- 所有时长统一显示为「**游戏天数（现实时间）**」，例如 `2.4 天（约 25 秒）`；不再出现「游戏分钟 / 游戏小时」这类小单位。
- 校队面板改成「已成立 → 可以组建 → 暂时不能成立」三组，每行两行小卡片（完整队名 + 状态），细节放进悬浮提示。
- 活动页分成「可以开展」与「暂时无法开展（默认折叠）」，折叠项直接写明缺什么条件。

### 修复

- **CI 用 Node 20 跑不动 jsdom 30**（`webidl.util.markAsUncloneable is not a function`）→ 工作流改用 Node 24，并在 `package.json` 声明 `engines`。
- **GitHub Pages 白屏**：工作流此前发布的是未内联的 `dist-single/index.html`（引用外部 `app.js` / `app.css`，而这些文件没有一起发布）→ 改为发布真正的单文件版 `outputs/星河实验学园.html`，并加了一道体积自检（小于 100 KB 就让构建失败）。
- 仓储资源顶到上限时不再反复弹提示（改为右栏「⚠ N 项接近上限」标签与悬浮明细）。

## 0.1.0 — 2026-09-22

首次公开：一款中文网页放置经营游戏，玩家扮演校长经营一所普通高中。

### 玩法与系统

- **核心循环**：建设校园 → 产生资源 → 安排教学 → 培养学生 → 解锁科技 → 解锁建筑 / 课程 / 校规 / 交流 → 参加活动与比赛 → 获得效果卡 → 学年结算 → 评价提升 → 学园传承 → 永久成长。
- **时间**：真实时间驱动的学年 / 学期 / 月份 / 日期 / 四季，季节影响产出、招生、比赛与考试；离线收益按 `lastSavedAt` 结算（上限 20 游戏日）。
- **建筑** 30+（成本递增、建造队列、仓储上限、类别联动），**课程** 30+（每门课并行覆盖全校、按覆盖率折算成长与消耗），**教师队伍**（能力 / 压力 / 士气 / 招聘与培训）。
- **学生群体模拟**：三个年级的聚合属性（学术 / 体育 / 艺术 / 科研 / 品德 / 社交 / 健康 / 创造力 / 压力 / 满意度），升学、毕业、转校友。
- **科技树** 5 大分支、**校规** 17 条（各有代价，名额有限）、**社团（部活）** 28 个、**校队** 17 支（训练 → 友谊赛 → 区 → 市 → 省 → 全国 → 国际，可改名、有退役与恋爱类事件）。
- **活动** 20+（比赛 / 交流 / 学园祭等祭典）、**效果卡** 40+（三选一）、**随机事件** 30+（选项写清代价与后果，支持延迟结算与预览）、**评级考核**（每次升档都要过评估）、**学年报告**、**校友**、**学园传承**、**成就** 40+、学校档案统计。
- **剧情引导 15 章** + 常驻任务面板；随机事件在引导期间不触发（只派发剧本事件），处理完第一件事件后恢复。

### 界面

- 三栏布局（校园信息 / 主视图 / 资源与学生状态）+ 底部导航，渐进式解锁（开局只开放总览、建筑、选项）。
- 列表统一「图标 + 名称 + 一行状态 + 一个按钮」，细节放进悬浮提示；事件、效果卡、评级考核、新解锁都用居中弹窗（统一 0.3 秒淡入淡出）。
- 内容全部数据驱动（`src/data/*`），并支持桌面「校园内容.js」热加载自定义内容，不需要重新打包。

### 工程

- TypeScript + Vite + Vue 3，无后端，可静态部署；`npm run build:single` 产出可双击打开的单文件 HTML。
- Vitest 测试 21 个文件 / 181 个用例（内容自检、公式、核心循环、存档与版本迁移、传承与离线、界面运行时、单文件版）。
- GitHub Actions：推送 `main` 自动测试 + 打包 + 发布 GitHub Pages。
