# 统一用 LF 存储，避免 Windows / macOS / CI 之间反复出现整文件差异
* text=auto eol=lf

# 二进制资源不做换行处理
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary

# 生成的单文件版：行太长，diff 没意义
outputs/星河实验学园.html -diff linguist-generated
